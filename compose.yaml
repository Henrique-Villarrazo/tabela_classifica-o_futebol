services:
  postgres:
    image: 'postgres:latest'
    environment:
      - 'POSTGRES_DB=broadcast_api'
      - 'POSTGRES_PASSWORD=postgres'
      - 'POSTGRES_USER=postgres'
    ports:
      - '5432'
